@model IEnumerable<ThAmCo.Events.Models.Staff>

@{
    ViewData["Title"] = "ListStaff";
}

<h2>ListStaff</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstAider)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@for (var i =0; i< Model.Count(); i++ ) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => Model.ElementAt(i).Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => Model.ElementAt(i).FirstAider)
            </td>
            <td>
                
            </td>
        </tr>
}
    </tbody>
</table>
